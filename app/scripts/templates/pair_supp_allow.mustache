<div id="main-content" class="card pair-auth">
    <header>
      <h1 id="fxa-pair-supp-allow-header">
        {{#unsafeTranslate}}Approve connection <small>to this device</small>{{/unsafeTranslate}}
      </h1>
    </header>

    <section>
      <div class="error"></div>
      <div class="success"></div>

      <form novalidate>
          {{{ unsafeDeviceBeingPairedHTML }}}

        <div class="button-row">
          <button type="submit" id="supp-approve-btn">{{#t}}Approve connection{{/t}}</button>
        </div>

        <div class="links">
          <a href="#" id="cancel">{{#t}}Cancel{{/t}}</a>
        </div>
      </form>
    </section>
  </div>
